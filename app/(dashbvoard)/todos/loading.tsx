'use client'

// Remember - loading indicator has to be a client component
const Loading = () => {
    return <div>loading content... (this artificial delay just to demo loading indicator)</div>
}

export default Loading