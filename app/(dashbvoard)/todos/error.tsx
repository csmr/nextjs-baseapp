'use client'

// Remember - loading indicator has to be a client component
// Filename matters, but 'Error' is already reserved in ecmascript.
// Remember, files are now route segments.
const ErrorPage = () => {
    return <div>Error page content.</div>
}

export default ErrorPage